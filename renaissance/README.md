# 文艺复兴 (Renaissance)

> 一个承载情感的音乐容器，让老歌的旋律在现代技术中通过"延迟满足"和"氛围重塑"重新焕发生命力。

## ✨ 核心特性

### 🎵 Lo-Fi 氛围引擎
- **三轨混音台**：旋律、炒豆声（黑胶噪音）、时空（环境音）
- **四种环境音**：1990年的街头、雨后的窗边、午后咖啡馆、深夜静谧
- **实时音效混合**：支持多音轨并行播放和独立音量控制

### 🎁 盲盒旋律播放模式
- **迷雾封层**：专辑封面随播放进度逐渐清晰
- **三阶段解锁**：
  - 阶段一 (0-50%)：全模糊，仅显示年份
  - 阶段二 (50-90%)：模糊度线性降低
  - 阶段三 (>95%)：完全清晰，触发"重逢成功"动画
- **动态粒子效果**：随节奏飘动的金色粒子

### 💌 黄金信件 (The Golden Letter)
- **PoL 解锁机制**：只有完整听完一首歌才能解锁信件
- **3D 翻转动画**：信封开启效果
- **手写信件样式**：复古纸张纹理和排版
- **随机偶遇**：无法搜索，只能随机发现他人的感悟

## 🎨 视觉设计

- **Fluent UI × Retro**：现代 Fluent 设计融合复古元素
- **暖色调色板**：奶油色、米色、深棕色、复古金
- **材质效果**：Mica 云母材质 + Acrylic 亚克力效果
- **物理阻尼动画**：拒绝线性动画，追求真实质感

## 🛠️ 技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | Flutter 3.x |
| 音频处理 | just_audio |
| 状态管理 | Riverpod |
| UI 组件 | fluent_ui |
| 动画 | flutter_animate |
| 本地存储 | ObjectBox |
| 后端 | Supabase |

## 📁 项目结构

```
lib/
├── core/
│   ├── theme/          # 主题配置
│   ├── constants/      # 常量定义
│   └── utils/          # 工具函数
├── features/
│   ├── player/         # 播放器核心
│   │   ├── audio/      # 音频控制
│   │   ├── ui/         # 播放器界面
│   │   └── models/     # 歌曲模型
│   ├── lofi_engine/    # Lo-Fi 氛围引擎
│   │   ├── mixer/      # 混音台
│   │   └── effects/    # 音效处理
│   ├── blindbox/       # 盲盒旋律模式
│   │   ├── blur/       # 模糊效果
│   │   └── unlock/     # 解锁逻辑
│   └── golden_letter/  # 黄金信件
│       ├── models/     # 信件模型
│       ├── repository/ # 数据仓库
│       └── ui/         # 信件界面
└── shared/             # 共享组件
```

## 🚀 快速开始

### 环境要求
- Flutter SDK >= 3.27.0
- Dart SDK >= 3.0.0

### 依赖版本说明

本项目使用以下关键依赖版本：

| 依赖包 | 版本 | 说明 |
|--------|------|------|
| fluent_ui | ^4.6.0 | UI 组件库 |
| intl | ^0.19.0 | 日期格式化（与 Flutter SDK 兼容） |
| flutter_riverpod | ^2.4.9 | 状态管理 |
| just_audio | ^0.9.36 | 音频播放 |

> **注意**：`intl` 包版本必须与 Flutter SDK 中的 `flutter_localizations` 兼容。Flutter 3.27.0 要求 `intl: ^0.19.0`。

### 安装依赖

```bash
cd renaissance
flutter pub get
```

### 解决依赖冲突

如果遇到版本冲突错误，请尝试以下步骤：

1. **清理依赖缓存**
```bash
flutter clean
flutter pub cache clean
```

2. **重新获取依赖**
```bash
flutter pub get
```

3. **如果仍有冲突，使用依赖覆盖**（临时方案）
在 `pubspec.yaml` 中添加：
```yaml
dependency_overrides:
  intl: 0.19.0
```

### Windows 项目配置

由于本项目手动创建了 Windows 桌面支持文件，在首次构建前需要确保以下文件存在：

#### 必需的文件结构
```
windows/
├── CMakeLists.txt                    # 项目级 CMake 配置
├── flutter/
│   ├── CMakeLists.txt               # Flutter 构建配置
│   ├── generated_plugins.cmake      # 插件配置
│   └── ephemeral/
│       └── generated_config.cmake   # Flutter 配置
└── runner/
    ├── CMakeLists.txt               # 可执行文件配置
    ├── main.cpp                     # 入口点
    ├── utils.cpp/h                  # 工具函数
    ├── win32_window.cpp/h           # Win32 窗口
    ├── Runner.rc                    # 资源文件（可选）
    ├── runner.exe.manifest          # 清单文件（可选）
    └── resources/                   # 图标资源
```

#### 首次构建步骤

1. **确保已启用开发者模式**
   ```powershell
   # 以管理员身份运行 PowerShell
   reg add "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /t REG_DWORD /f /v "AllowDevelopmentWithoutDevLicense" /d "1"
   ```
   或通过设置界面：设置 → 隐私和安全性 → 开发者模式 → 开启

2. **生成完整的 Windows 项目文件（推荐）**
   ```bash
   # 这将补全所有缺失的文件
   flutter create --platforms=windows .
   ```

3. **获取依赖**
   ```bash
   flutter pub get
   ```

4. **构建 Windows 应用**
   ```bash
   flutter build windows
   ```

#### 常见问题

**问题 1：CMake Error - 缺少 CMakeLists.txt**
- 解决：运行 `flutter create --platforms=windows .` 生成完整项目文件

**问题 2：需要符号链接支持**
- 解决：启用 Windows 开发者模式（见步骤 1）

**问题 3：插件构建失败**
- 解决：确保 Visual Studio 2022 已安装"使用 C++ 的桌面开发"工作负载

### 运行应用

```bash
# Windows
flutter run -d windows

# macOS
flutter run -d macos

# Linux
flutter run -d linux
```

### 构建发布版本

```bash
# Windows
flutter build windows

# macOS
flutter build macos

# Linux
flutter build linux
```

## 📝 使用说明

1. **选择歌曲**：在左侧列表中选择一首老歌
2. **开始播放**：点击播放按钮，封面开始模糊
3. **调整氛围**：点击右上角混音台图标，调整 Lo-Fi 效果
4. **等待解锁**：随着播放进度，封面逐渐清晰
5. **查收信件**：歌曲播放完毕后，如果有黄金信件会弹出通知
6. **阅读感悟**：点击信件徽章，3D 翻转查看陌生人的故事

## 🎨 设计哲学

- **温度 (Warmth)**：拒绝冰冷的数字化列表，模拟物理世界的阻尼感和材质感
- **仪式 (Ritual)**：听歌不仅仅是点击，而是通过"等待"和"探索"获得共鸣
- **纯粹 (Purity)**：弱化社交压力，强化人与旋律、人与文字的跨时空连接

## 📄 许可证

MIT License

---

*"音乐是时间的艺术，而我们要做的是让时间有温度。"*
